# S3 configuration
aws.s3.bucket-name=${AWS_S3_BUCKET_NAME:demo-mocknest-mappings}

# SES configuration
aws.ses.sender-email=${SENDER_EMAIL:docsflow@vintik.nl}
aws.ses.recipient-email=${RECIPIENT_EMAIL:elenavanengelen@vintik.nl}

# API Configuration (backward compatibility - prefer using application.yml)
api.bored.url=${BORED_API_URL:https://bored-api.appbrewery.com/filter?type=social}
api.bored.token=${BORED_API_TOKEN:}
api.petstore.url=${PETSTORE_API_URL:https://petstore.swagger.io/v2}
api.petstore.token=${PETSTORE_API_TOKEN:}
